function LoginButton(props) {
    return <div onClick={props.onClick}>로그인</div>;
}

function LogOutButton(props) {
    return <div onClick={props.onClick}>로그인</div>;
}

const Buttons = {
    LogOutButton,
    LoginButton,
};

export default Buttons;
